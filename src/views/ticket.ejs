<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy Ticket</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div class="container">
        <h1>🎟️ Buy Your Loto Ticket</h1>

        <% if (user) { %>
            <form name="roundForm" action="/save-ticket" onsubmit="return validateForm()" method="POST" class="ticket-form">
                <div class="form-group">
                    <label for="card_id">Enter your Card ID:</label>
                    <input type="text" name="card_id" id="card_id" placeholder="e.g. 12345" required>
                    <p class="error-message" id="cardError"></p>
                </div>

                <div class="form-group">
                    <label for="ticket_numbers">Enter your Round Numbers:</label>
                    <input type="text" name="ticket_numbers" id="ticket_numbers" placeholder="e.g. 3,7,12,22,35,44" required>
                    <p class="error-message" id="numbersError"></p>
                </div>

                <button type="submit" class="btn primary">Submit</button>
            </form>
        <% } else { %>
            <p class="info-text">Please <a href="/login" class="link">login</a> to buy a ticket.</p>
        <% } %>
    </div>

    <script src="/scripts/formValidation.js"></script>
</body>
</html>
